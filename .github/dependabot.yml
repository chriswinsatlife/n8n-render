# .github/dependabot.yml

# This file tells Dependabot what to check for updates.
version: 2
updates:
  # Scans the n8n Dockerfile for base image updates.
  - package-ecosystem: "docker"
    directory: "/"
    schedule:
      interval: "monthly"
    open-pull-requests-limit: 3
